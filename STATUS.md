# STATUS

> The single source of truth for project state. Every coding session reads this and `project.md` first, then updates this file last.

## Current phase

Phase 3 — approved UI-review improvements (navigation, drawer IA, map-state surfaces, half-height detail sheet, landing compression)

## Done

- Preserved the original regular and tutorial applications unchanged at the repository root and as byte-identical copies under `original/`.
- Added `index.html`, a minimal CHIP-branded landing page with tutorial and dashboard choices.
- Added versioned first-time visitor state with guarded `localStorage` access (`chip:tutorial-completed:v1`).
- Added `app.html` and `tutorial.html` as full-viewport same-origin shells around the original source files.
- Added mobile device classification using viewport, pointer, touch, and User-Agent Client Hints when available.
- Reconciled the existing mobile controls mismatch around the `open` state without changing the source application.
- Added a mobile controls backdrop, close action, Escape support, focus containment, safe-area handling, dynamic viewport sizing, larger touch targets, and detail-sheet constraints.
- Added tutorial completion detection for explicit finish actions and guided-interface closure after real tutorial interaction.
- Added map resize signaling after panel, viewport, orientation, and visibility changes.
- Replaced the Python-template documentation with project-specific `README.md` and `CLAUDE.md` guidance.
- Replaced the release-oriented workflow with static validation and added a GitHub Pages Actions deployment workflow.
- Added repository-level static tests in `tests/test_site.py`.
- Opened draft pull request #1 from `agent/github-pages-mobile-demo` into `main`; merged to `main`.
- Validation completed: local `pytest -q` passed 11 tests, all three browser scripts passed `node --check`, and pull-request CI run #7 completed successfully.
- Fixed the mobile initial map view (branch `claude/mobile-tutorial-ui-bugs-6yi1qf`): the original apps fit `BOUNDS` with 360/380px desktop-panel padding, which exceeds a phone-width map and made MapLibre fall back to a zoom-0 world view. `mobile-ui.js` now injects a one-shot script into the child document (needed because `map`/`BOUNDS` are top-level lexical bindings, not `window` properties) that re-fits the market bounds with phone-sized padding on mobile only.
- Fixed the mobile tutorial layout (same branch): the tour card is now a fixed bottom sheet on mobile (safe-area aware, scrollable copy, 44px buttons always on screen), and `mobile-ui.js` auto-opens the controls drawer during tour steps 1-4 (whose targets live inside the off-canvas drawer, previously producing a sliver focus ring at the left edge and a card cut off below the viewport), scrolls the step target into view, re-renders the tour after the drawer transition, and closes the drawer for the map step and at tour end. The drawer height is capped while the tour is open so drawer and card do not overlap.
- Validated the fixes in headless Chromium (iPhone 13 viewport): map zoom 6.66 centered on Cleveland-Akron on both `tutorial.html` and `app.html`; tour steps 1-5 all render the card fully on screen with Next reachable; drawer opens/closes per step; desktop run (1440x900) confirmed unchanged behavior (no injected fit, original camera and card positioning).
- Made mobile tour step 6 ("Review the detail panel") show the real detail sheet: the delivery layer injects helpers that call the original `selectCounty`/`deselect` functions with the Cuyahoga sample county when the step opens (only if no county is already selected), moves the tour card to the top of the screen for that step, caps the sheet height so card and sheet never overlap, and deselects when leaving the step or ending the tour. Previously the step fell back to highlighting the app bar because the detail sheet is hidden on mobile until a county is tapped.
- Kept county labels visible when zooming out on mobile: the original `positionLabels` hides all labels below zoom 7.2, but the mobile full-market view sits near zoom 6.7, so the default view showed none. `mobile-ui.js` now injects a mobile-only tuner that wraps `positionLabels` (and adds its own move/zoom listeners) to keep labels visible down to zoom 5.2, growing them from 11px up to 13.5px as the map zooms out (inline `font-size` set with `important` priority because the mobile stylesheet pins `.clab` to 11px with `!important`), and hiding them below zoom 5.2 where the market is too small to label. Behavior at zoom 7.2 and above is unchanged, desktop is untouched.
- Made the desktop tour responsive to short screens (small laptops): the original tour never scrolls a step target into view or keeps its card inside the viewport, so on short viewports steps 3-4 highlighted content below the panel fold (no visible ring) and the card ran off the bottom of the screen. `mobile-ui.js` now, on desktop, scrolls panel-hosted step targets into view (`scrollIntoView` block "nearest") before re-rendering the ring, and clamps the tour card's inline position into the viewport (comparing the intended inline `top`/`left` rather than the transitioning rect, and only rewriting when the card actually overflows, so full-size desktop layouts are pixel-identical). A style observer on the card keeps the clamp applied after the tour's own scroll/resize re-renders. Updated the `test_mobile_tour_adapts_to_short_screens` observer-count contract (4 to 5) and added assertions for the desktop reveal and clamp.
- Investigated the report that the desktop landing page is missing on the deployed site: could NOT reproduce a repo-level defect. `index.html`, `landing.js`, and `tutorial-state.js` are unchanged since their initial commit, all six Pages deploy runs succeeded with the latest deploying current `main`, the deploy artifact copies `index.html` to the site root, and a local desktop render of `main` shows the landing page correctly. Most likely a client-side cause (cached deep link, bookmark, or browser autocomplete going to `app.html`/`CHIPv.4.2.html` directly).

- Completed the approved 2026-07-19 UI review (branch `claude/ui-mobile-desktop-review-xq1l6m`), one commit per phase, all in the delivery layer:
  - Phase A — the mobile controls drawer opens straight onto the controls: injected "Map controls" header with a 44px close button, the market-intro `.hdr` copy moved below the controls with flex `order` (DOM order untouched so tour selectors keep matching) and collapsed behind an "About this market" toggle, a "Replay the guided tour" link (dashboard only, `target="_top"`), and a persistent sample-data footer note. Tour step 1 auto-expands the collapsed copy and restores it afterwards. Replaced the old "no injected close button" test contract, which this review reversed.
  - Phase B — mobile map-state surfaces: a bottom-left legend chip mirroring the drawer's metric label, color ramp, min/max and panel mode (hidden while the drawer, tour, or detail sheet is open, and never shown if the app's UI fails to build), plus an active-filter count badge on the Controls button with a matching aria-label. Child-window MutationObserver count contract is now six.
  - Phase C — the mobile detail sheet opens at 55dvh with an injected grab handle (tap, keyboard, or short drag) expanding to 84dvh (88dvh under 480px); each fresh selection resets to half height; the tour detail-step height caps still win; the original `.open` display toggle is untouched.
  - Phase D — approved desktop-visible navigation: the app-bar brand acts as a link to `index.html` on both pages (pointer cursor, native tooltip, focus ring; no resting visual change), phones get a 44px Home chip in the app bar, and desktop `app.html` gains a "? Take the tour" launcher cloned from the tutorial page's `.tour-launch` styling (hidden on mobile).
  - Phase E — landing-page phone compression in `landing.css` only: phone-scale hero type, tighter cards without the decorative icon, left-aligned recommendation badge under 430px, and the non-recommended card condenses to kicker + title + action (follows the `is-recommended` class landing.js moves, so the returning-visitor state condenses the tutorial card instead).
- Collapsed the map attribution pill on phones: MapLibre's compact attribution starts expanded ("© OpenStreetMap contributors © CARTO" across the bottom of the map). The delivery layer now collapses it to its info toggle at load and after viewport changes, using the control's own button so internal state stays consistent; the required attribution remains one tap away, an explicit user toggle (trusted events only) is never overridden, and desktop keeps the original always-visible strip.
- Added the CHIP assistant chat to `app.html` (`assets/js/chip-assistant.js`, `assets/css/assistant.css`): an "Ask CHIP" launcher opens a chat that answers questions like "How is Summit County doing?" or "Top prospects in Erie" and drives the real dashboard (selects the county, switches AE/Prospecting mode). With an Anthropic API key it calls the Claude API directly from the browser (raw `fetch`, `anthropic-dangerous-direct-browser-access` header, default model `claude-opus-4-8` with a Haiku option) using tool use over a facade injected into the child document that reads the app's own `GEO`/`CLIENTS`/`PROSPECTS`/`BUDGETS`/`TAILWIND_RANK` bindings — so answers are grounded in the page's illustrative sample data, never invented. **No key is ever committed**: the presenter pastes a key at demo time (session storage only, guarded, with guidance to use a spend-capped workspace key); without a key the chat answers county-overview and top-prospect questions from the local data in a labeled demo mode, so the public deployment never shows a broken chat. The panel is a bottom sheet on phones, docks beside the county detail panel on wide desktops, and the tutorial page stays chat-free. Handles refusal/tool-use/max-tokens stop reasons, 401/429/5xx/network errors, and works even if the basemap CDN is down (map focus degrades silently, data answers still work).
- Tightened assistant answers after user feedback that they were long and dense: every reply now leads with a one-sentence **bold** takeaway that interprets the data (ahead/behind plan, who to call first), followed by at most three short bullets with rounded numbers; prospects default to 3 with one hook each (vertical, ~budget, current holder) instead of four scores; two-county questions get a compact side-by-side instead of two full dumps. Claude mode enforces the same style via the system prompt (bold + "• " bullets only, under 70 words); bubbles render **bold** through a text-node-only formatter so replies can never inject markup.
- Made the walkthrough read as one continuous nine-step tour (2026-07-20 device feedback): the frozen tour's "Step N of 7" kicker, seven progress dots, and step-7 "Finish" label are relabeled after every render via a kicker MutationObserver ("Step N of 9", nine dots, "Next"), the two delivery-layer steps show "Step 8/9 of 9" with the real Finish on step 9, Back from step 8 replays the main tour synchronously to land on step 7 (single paint), and a "Skip tour" button matches the original actions row. Tutorial completion now records at the real Finish (step 9) through the existing text-pattern listener. Child-window observer contract is now seven.
- Rearranged the floating actions into one right-edge ladder after device feedback that Ask CHIP and the replay chip collided with the zoom UI: bottom-up it reads attribution toggle, zoom, Ask CHIP, then the tour/Tutorial chip — desktop offsets 140/190px (chat panel raised to match), phone offsets 160/214px + safe area (the compact attribution toggle sits inside the same MapLibre corner container, so the zoom group tops out near 150px). The tutorial page's own `.tour-launch` joins the ladder via a delivery-layer override of its frozen position, and the tour's bonus-step rings follow automatically because targets are measured live.
- Fixed desktop tour step 6 ("Review the detail panel"), reported from the deployed site: with no county selected it fell back to highlighting the app bar. The desktop sync branch now drives the same injected `__chipTourShowDetailSample()` helper the mobile path uses — Cuyahoga is selected when the step opens (only if nothing is already selected), the ring re-renders onto the open panel, and the sample clears on step change or tour end; a user's own selection is never touched. Verified in headless Chromium: ring on the panel, step-7 cleanup, Back re-select, and a pre-selected Stark preserved through and after the step.
- Added two bonus tour steps after the original tour's Finish click — "Ask CHIP anything" (spotlights the assistant launcher) and "Replay this tour anytime" (spotlights the Tutorial button). The original steps array is closure-local inside the frozen tutorial file, so the extension is a delivery-layer sibling overlay (`chipTourExtra`, built by `setupTourExtension` in mobile-ui.js) that reuses the tour's own `.tour-shade`/`.tour-focus`/`.tour-card` classes and renders identically; it starts only after a Finish click (Skip/Escape/Close don't trigger it), keyboard works (arrows/Enter/Escape), and on mobile the card moves to the top so the bottom-right targets stay visible. The assistant now loads on the tutorial page too (so the bonus step highlights a real button), and mobile-ui.js hides the launcher while the original tour runs since it floats undimmed above the tour shade. Completion recording is unaffected (marked at Finish, before the bonus steps).
- Optimized mobile typography for the chat and pop-up surfaces after user feedback: chat panel body 14.5px/1.55 with roomier bubbles and chips, settings inputs at 16px (prevents iOS focus zoom), tour card copy 14px/1.55 with a 20px title, and the injected drawer furniture (header, About toggle, replay link, sample-data note) up 1px each. Desktop sizes unchanged.
- Gave the two floating launchers real icons so they read as UI elements: "Take the tour" now carries an aqua play-in-circle SVG and "Ask CHIP" a chat-bubble-with-sparkle SVG (both inline, stroke/fill from currentColor with the product's aqua accent; aria-hidden, labels unchanged).
- Completed the 2026-07-20 "final touch ups" round in a single new delivery-layer module, `assets/js/app-refinements.js` (loaded by `app.html` and `tutorial.html`; frozen originals and the mobile-ui.js observer-count contract untouched):
  - Removed the "Black / African American" and "Hispanic population index" metrics from both the "Color counties by" dropdown and the detail-pane demographics table, by splicing the child's `METRICS` array from an injected script before `buildUI()` runs (with a defensive option sweep if the map loaded first).
  - Desktop county labels now scale with zoom: an injected wrapper around `positionLabels` (same pattern as the mobile tuner, which is unchanged and still owns phones) grows labels from the frozen 12px baseline at zoom 7.2 toward 19px as the user zooms in; visibility rules unchanged.
  - The Methodology and "Get / Keep / Grow — how & why" dropdowns moved out of the control panel into a popup that opens where the detail pane sits. The popup borrows the frozen `.panel`/`.detail` classes so geometry, theming and the phone bottom-sheet treatment come for free; the app bar's Methodology button is rewired to open it (inline onclick stripped), Escape and a close button dismiss it, and phones — where the app nav is hidden — get a "Methodology & framework" drawer link in the dropdowns' old slot.
  - Clicking a county gently zooms (+0.55, capped at 8.4, never out) and centers it via a wrap of `selectCounty`, offset so the county stays visible beside the desktop detail pane / above the mobile sheet; MapLibre treats the ease as non-essential so reduced-motion users get an instant move.
  - The AE client book ranks by priority — churn tier (the app's own High/Med/Low cut points), KEEP before GROW within a tier, then spend — via a wrap of the frozen `clientRowsHTML`, with an explanatory note above the table; the KEEP pill is now red for urgency.
  - The long detail-pane sections (Demographics, Sales snapshot, Sales plan, Revenue by vertical, Client book, and the prospecting blocks) collapse into dropdowns built from each block's own `.block__t` heading; "Market read" (AE) and "Priority Get targets" (Prospecting) start open, per-section open state is remembered across re-renders for the visit.
  - The floating "Take the tour" / "Ask CHIP" chips no longer cover an open pane: a pane watcher measures the open detail pane or methodology popup and shifts the chips left of it on desktop (`--chip-pane-clear` on both document roots, 200ms ease); phones hide the chips while a full-width sheet is up. Suppressed while the tour runs so highlight rings stay on target, and re-synced when the tour ends.
  - The mode switch now sits at the top of both panes: the frozen "Switch to the Prospecting Tool" button moves up under the county header (its listener survives the move), and the prospecting pane — which previously had no way back — gains a matching "Switch to the AE Interface for {county}" button.
  - Validation: `pytest -q` 25 passed (two new contracts for this round plus the new file in the required-files, no-keys and no-hardcoded-owner sweeps); `node --check` clean; headless Chromium (stubbed tiles) desktop 1440×900 — 16 behavioral checks including metric removal, label scaling up/down, ease-to zoom+center, section dropdowns with remembered state, churn-tier-monotonic client rows, red KEEP pill, chips clearing the pane, popup open/close/Escape with both reference dropdowns moved and the control panel emptied of them — and iPhone 390×844 — drawer link opens the bottom-sheet popup, Ask CHIP hides while a sheet is up and returns after, mobile label tuner still pins 11px at market zoom; full nine-step tour regression re-run clean on both devices plus the app.html ladder.
- Added a "Rank by" toggle to the AE client book (follow-up to the priority ranking): two pills, **Churn risk** (the defend-first default — churn tier, KEEP before GROW, spend) and **Growth upside** (the app's own `postureScore` descending — positive means wallet room and momentum — then spend). The choice re-renders through the frozen `refreshOpenDetail()` pipeline, is remembered for the visit across county changes and re-renders, updates the explanatory note, and keeps the section's open state. Validated in headless Chromium: default churn order tier-monotonic, growth order verified to exactly match a `postureScore`-descending sort of the county's own `CLIENTS` data (DOM names compared to data-side expectation, not display parsing), toggle state and order both survive switching counties; `pytest -q` 25 passed with the new sort-bar contracts; nine-step tour and full touch-up regressions re-run clean.
- 2026-07-20 follow-up round (green switch, mobile map fix, desktop/mobile parity):
  - The prospecting pane's "Switch to the AE Interface" button is now green (`chip-btn-ae`, #15803d with white text, `!important` over the frozen theme recolors) so the two switch directions read differently at a glance — the way to Prospecting stays the original purple.
  - Fixed the deployed-iPhone report of the tutorial sitting on a world-view map: the frozen constructor fits the market with 740px of desktop-panel padding, which fails on phones, and the delivery layer's single refit at iframe load proved racy on real devices. `applyMobileMapFit` now re-asserts the market view after style load, on style idle, and on a 0.8/2/4.5/8s backoff — refitting only while the camera is clearly at world scale (zoom < 4), the device is in mobile mode, and the user has not touched the map (dragstart/wheel/touchstart/dblclick disarm it permanently).
  - Mirrored the desktop label scaling to phones: above the 7.2 baseline mobile county labels now grow from their pinned 11px toward 16px as the user zooms in (`important` priority to outrank the mobile stylesheet); below 7.2 the existing mobile-ui.js zoom-out tuner keeps sole ownership. The refinement runs last in the `positionLabels` chain and listener order, so the two tuners never fight.
  - Mobile-parity polish: accordion section heads get 44px tap targets on phones (all other touch-up features — popup sheet, sort toggle, switch bar, chip hiding — were already mobile-adapted).
  - Validation: `pytest -q` 26 passed (new map-fit and parity contracts); headless Chromium — mobile tutorial loads on the market view, a simulated no-gesture world-view fallback self-heals at the backoff checkpoint, a user-gesture camera is never overridden, mobile labels 11px at 7.2 / ~14px at 8.8 / tuner-owned below 7.2, green button verified computed rgb(21,128,61) on both form factors, desktop label scaling regression clean; full touch-up, nine-step tour, and sort-order suites re-run green.
- Cleaned up the AE detail-pane hierarchy (2026-07-20 feedback round with photo — "important things front and center"): **Sales snapshot** and **Sales plan** are no longer dropdowns and sit permanently visible directly under the county header and switch button; the **Client book** (with its rank toggle) follows them, open by default; the long **Market read** blurb collapses to one tap; and **Demographics + Revenue by vertical/By channel merge into a single collapsed "County data · demographics & mix" dropdown at the bottom** (the source blocks' frozen headings become sub-heads inside it). Kept the dropdown pattern rather than introducing a tab system so the pane stays one consistent interaction. The prospecting pane is unchanged. Also hardened the mobile market fit further: the world-view reproduced once in the sandbox (the initial refit can run before the mobile stylesheet lays the map container out full-width, so `fitBounds` throws), and since the container correction always fires a map `resize`, the ensure now also runs on `resize` and heals that race immediately instead of at the next backoff checkpoint. Validation: `pytest -q` 26 passed (updated section-hierarchy contracts); headless Chromium confirms the exact section order (snapshot → plan → book → county data last), snapshot/plan absent from the accordion set, merged sub-heads present, remembered open-state, and all prior suites (touch-ups, nine-step tour, sort order, round-3) re-run green.
- 2026-07-20 bug-fix and polish round (three device photos):
  - Prospecting pane: **County spend** now leads with **County tailwind** directly below, and both are always visible (added to the never-collapse set); the order swap is a delivery-layer DOM move at render time.
  - Floating chips vs panes, redesigned: the desktop "shift left of the pane" turned out fragile (chips stranded mid-map after open/close cycles — the field photos) and ugly. Both **Take the tour / Tutorial and Ask CHIP now simply hide while any pane is open** (county detail or methodology popup) on both devices and restore on close. Hardened every path that toggles the state: each enhancement step in app-refinements.js is individually try/caught (a dead pane watcher was the likely stranding mechanism), the frozen `deselect()` is wrapped to re-sync directly, stale observers from a reloaded iframe self-disconnect, and the parent-side observer is a single swapped slot.
  - Desktop county labels no longer vanish when zooming out (both HTMLs): below the frozen 7.2 cutoff they stay visible at a shrinking size (12px → 9.5px floor) down to zoom 5.2, hidden only when the market is truly tiny — mirroring the phone behavior. Zoom-in growth unchanged.
  - Suppressed the stranded hover tooltip on phones (`#tip` display:none under `data-device="mobile"`): a tap selects a county, it never needs a hover card, and iOS left the last one pinned to the map.
  - **Cache-busting**: every local CSS/JS reference on index/app/tutorial now carries `?v=20260721` (one shared value, asserted by a new test), and mobile-ui.js forwards the version to the stylesheet it injects into the iframe — field debugging pointed to stale-CSS/fresh-JS cache splits producing mixed chip states on deployed devices.
  - Validation: `pytest -q` 27 passed; new Playwright pane/chip suite covers the exact reported flows on tutorial.html and app.html desktop (chips at ladder → hidden while prospect pane open → restored at ladder after the pane's own ✕, same for the methodology popup) plus mobile (tooltip suppressed even when forced, Ask CHIP hides with the sheet and returns, County spend first on the sheet); all prior suites (touch-ups, nine-step tour, sort order, round-3) re-run green.
- Moved the Ask CHIP launcher to the top-right on phones (2026-07-20 photo: it floated over the open drawer, covering the "Replay the guided tour" row): it now mirrors the Controls chip under the app bar (top 72px + safe area, right 12px), and the near-fullscreen drawer counts as an open pane, so the launcher hides while the drawer, detail sheet, or methodology sheet is up and returns on close. The pane watcher observes `#ctrl`'s class for this; only real panes still feed the chat panel's beside-detail offset. The tour's bonus card now pins opposite its target on phones (bottom sheet for the relocated top-right launcher on step 8, top-of-screen for the bottom replay chip on step 9) instead of always pinning to the top. Desktop launcher unchanged (bottom ladder). Both HTML pages verified: launcher at top-right clear of the app bar and Controls chip, drawer hides/restores it with the replay affordance visible (dashboard drawer row; tutorial's own chip after close), bonus steps 8/9 rings land on their targets with the card clear, desktop ladder untouched; `pytest -q` 27 passed; pane/chip, touch-up, nine-step and round-3 suites re-run green.
- 2026-07-21 app-bar tabs round: the floating "Take the tour" / "Ask CHIP" pills kept colliding with panes on desktop (photos), so their jobs moved into the app bar as tabs — order **Market map · Take the tour · Ask CHIP · Open Architect · Open Salesforce · Methodology** (Methodology moved to the end per feedback). "Take the tour" navigates to the tutorial from the dashboard and replays the walkthrough on the tutorial page; "Ask CHIP" toggles the chat panel (the hidden launcher keeps owning panel state; the tab routes a second press through the panel's close control); the two demo-connection tabs open a centered dialog — "This is a demo of how CHIP could connect — in a production build this button opens straight to your Architect/Salesforce dashboard…". Desktop floating pills retired (injected dashboard pill hidden everywhere; the tutorial's frozen chip and the parent Ask CHIP pill are mobile-only now); tabs compress slightly under 1280px; the app nav is force-hidden in mobile mode (the frozen CSS only hides it ≤820px but coarse-pointer tablets classify mobile to 1100px). Phones keep their own affordances — top-right Ask CHIP pill, drawer replay row, tutorial chip — and gain "Open Architect" / "Open Salesforce" drawer rows in the methodology link's group; the connect dialog counts as an open pane so the mobile launcher hides behind it. The tour's bonus steps follow the relocated targets (desktop rings land on the new tabs, phones keep the pill/chip; step-9 copy updated). Validation: `pytest -q` 28 passed; new Playwright tab suite green on both pages and devices (tab order, pill retirement, chat toggle, both dialogs with Escape/Close, Methodology-from-the-end, tour replay from the tab, bonus-step rings on the tabs, mobile drawer rows and dialog-vs-launcher interplay); all five prior suites re-run green after updating their retired-chip expectations.
- Tour audit after the app-bar-tabs round: the frozen seven-step tour's targets all remain valid (steps 1–4 panel controls, step 5 map area, step 6 detail pane, step 7 the app nav — which now shows every new tab inside the ring), and the two delivery-layer bonus steps already ring the new tabs. Two pieces of stale frozen copy are now patched at render time: step 1's "replay it from the Tutorial button" becomes "replay it from the “Take the tour” tab up top — on phones, “Replay the guided tour” in the Controls drawer" (patched in relabelMainTour, so it reapplies on every render), and the client book's posture note "See Get / Keep / Grow — how & why in the left panel" becomes "…under Methodology in the top bar (Controls drawer on phones)" (text-node-only surgery in the detail pipeline). Verified in headless Chromium: both patches present, step-1 patch survives Back/Next re-renders, nine-step tour and tab suites re-run green; `pytest -q` 28 passed.
- Locked the tutorial page's mobile "? Tutorial" replay chip to the top rail (2026-07-21 device photo: it was stranded mid-map at the old bottom-ladder offset once Ask CHIP moved up): it now stacks directly under Ask CHIP at top-right (safe-top + 126px, right 12px, 44px min height), hides with the same pane rules (drawer/sheets/dialog), and the tour's bonus step 9 rings it with the card pinned below. Verified: rail geometry (Controls left; Ask CHIP then Tutorial right, 10px gap, no overlaps), hide/restore cycle returns it to exactly top 126, bonus-step ring centered with the card clear; `pytest -q` 28 passed; nine-step, launcher, pane/chip, and tab suites re-run green.
- Fixed the recurring mobile world-view bug for good (2026-07-21 field photo: world map after completing the tour, on a build that already had the top-rail chip, i.e. all previous safeguards). Root cause was two-fold: (a) any touch on the map permanently disarmed the auto-fit — and tour step 5 invites a map tap — and (b) every retry expired within 8 seconds of load, so nothing could heal late breakage. The injected fit is now a perpetual watchdog: every 2 seconds (plus the resize/load/idle hooks), a camera below zoom 4 in mobile mode is re-fitted to the market unless a user gesture happened in the last 10 seconds — active pinching keeps deferring it, but nobody deliberately studies a 17-county dashboard from world scale, so a stale world view always heals. Additionally, selecting a county from a broken camera (which is what the tour's detail-sample step does) now goes straight to county zoom 7.8 instead of easing to "broken + 0.55". Verified the exact field scenario in headless Chromium: stale-gesture world view heals via the watchdog, a fresh gesture is respected inside the grace window and healed after it, county-select from world view lands at 7.8; `pytest -q` 28 passed; round-3, nine-step, top-rail and touch-up suites re-run green.
- Validation for the earlier phases: `pytest -q` 21 passed (new contracts: assistant wiring, no committed credentials anywhere in the delivery layer); `node --check` clean on all three browser scripts; headless Chromium sweep (CARTO tiles stubbed for determinism) at 320×568, 360×800, 375×667, 390×844, 393×852, 412×915, 430×932, 768×1024, and 844×390 landscape — no horizontal scroll, 44px targets, drawer open/close via the new button, legend chip visible, sheet opens at half height inside the viewport; full 7-step mobile tour walkthrough with completion recorded; desktop 1440×900 and 1280×720 — every mobile-only element `display:none`, panel copy and layout unchanged, the only visible desktop additions being the two approved phase D affordances. Landing: primary CTA above the fold at 320×568/375×667/390×844, both CTAs plus the disclaimer on one screen at 390×844, desktop landing byte-identical CSS path.

## Next up

1. Re-verify the tutorial, initial zoom, and the new drawer/sheet/legend surfaces on physical iPhone Safari and Android Chrome after this branch deploys.
2. Complete remaining visual QA on iPad Safari and representative desktop sizes.
3. Record any device-specific defects here before making follow-up changes.

## Decisions log

- 2026-07-17 — Keep the original HTML files byte-for-byte unchanged — protects the existing desktop design and provides a clean rollback baseline.
- 2026-07-17 — Use same-origin wrapper pages for the active dashboard and tutorial — allows a separate mobile delivery layer without rewriting two very large self-contained HTML files.
- 2026-07-17 — Recommend rather than force the tutorial — preserves presenter and returning-user control.
- 2026-07-17 — Deploy through GitHub Actions — matches the repository's configured GitHub Pages source.
- 2026-07-17 — Retain pytest only for static repository validation — no Python runs in the deployed application.
- 2026-07-17 — Fix the mobile map view and tutorial layout entirely in the delivery layer (`assets/js/mobile-ui.js`, `assets/css/mobile.css`) — the original HTML files stay byte-identical. The tour-step drawer targets (`.hdr`, `#modeSeg`, `.field`, `#dmaClientFilters`) are mirrored in `mobile-ui.js`; if the frozen source ever changes step order, unmatched selectors are ignored harmlessly.

- 2026-07-19 — Reverse the earlier "no injected close button" decision: the approved UI review adds a visible drawer header with a 44px close button (`chip-drawer-close`), and the test contract now asserts its presence.
- 2026-07-19 — Desktop-visible additions are limited to the two affordances approved in the review: the brand-as-home link (no resting visual change) and the `? Take the tour` launcher on `app.html` mirroring the tutorial's own launcher. Everything else ships mobile-scoped.
- 2026-07-19 — Reorder the drawer visually with flex `order` instead of moving DOM nodes, so the frozen tour step selectors and app listeners stay valid.
- 2026-07-20 — Assistant chat calls the Claude API directly from the browser with a runtime-entered key instead of an embedded key or a proxy backend: GitHub Pages is public (a committed key would be scraped) and the project forbids backends. The keyless local fallback keeps the public site functional. Raw `fetch` rather than an SDK keeps the delivery layer dependency-free per CLAUDE.md.
- 2026-07-20 — The "final touch ups" round ships in a new module (`assets/js/app-refinements.js`) instead of growing `mobile-ui.js`: it keeps the observer-count test contract stable and separates this round's dashboard-behavior work from device-adaptation work. Wrapping strategy: top-level `function` declarations in the frozen originals (`renderDetail`, `clientRowsHTML`, `selectCounty`, `positionLabels`, `setMode`) are global bindings and are wrapped — from the parent where no `const`/`let` access is needed, from an injected child script where it is (`map`, `GEO`, `METRICS`).
- 2026-07-20 — Client-book "priority" is defined as churn tier first (the app's own ≥22%/≥12% High/Med cut points), KEEP before GROW within a tier, then spend — revenue in danger outranks upside, matching the KEEP-is-urgent red accent the user asked about.
- 2026-07-20 — User-requested desktop changes (scaling county labels, removed metrics, relocated methodology dropdowns, red KEEP pill, reordered client book, collapsible detail sections, relocated switch buttons) are explicit scope expansions of the "no desktop changes" default, requested in the 2026-07-20 feedback round with photos.

## Noticed

- The original application depends on external map/font/library resources, so fully offline rendering is not expected.
- The original app builds its controls UI only after the basemap style loads (`buildUI()` runs inside `map.on("load")`): if the CARTO CDN is unreachable, the panel renders with an empty metric select and no message. Fixing this would require editing the frozen originals, so it is recorded here instead; the delivery layer's legend chip guards against it by staying hidden until the metric select has options.
- Static CI cannot replace physical-device visual testing of mobile Safari browser chrome and safe areas.

## How to run

```bash
python -m http.server 8000
```

Open `http://localhost:8000/`.

## How to validate

```bash
python -m pip install -r requirements.txt
pytest
```
